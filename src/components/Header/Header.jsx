import React from 'react';
import NavMenu from '../NavMenu/NavMenu';

const Header = () => {
    return (
        <header className='bg-[#faf9ff]'>
            <NavMenu />
        </header>
    );
};

export default Header;